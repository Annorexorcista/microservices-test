server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/Annorexorcista/microservices-test
          search-paths: config-data
  application:
    name: config-server